import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "STEIGERN - Design in Motion | Automatización Industrial",
  description: "Líderes en automatización industrial. Desarrollamos e integramos soluciones de conveyor, estaciones automatizadas y sistemas semi-automatizados con precisión alemana.",
  keywords: ["automatización industrial", "conveyor", "sistemas automatizados", "integración industrial", "manufactura", "industria 4.0"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}